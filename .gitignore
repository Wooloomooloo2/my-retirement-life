# Virtual environment
.venv/
venv/
env/

# Python cache
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.pyc

# Distribution / packaging
dist/
build/
*.egg-info/

# Environment variables - never commit secrets
.env

# User data - personal triple store data stays local
data/

# PyInstaller build output
*.spec
!tools/MyRetirementLife.spec
*.exe

# VS Code
.vscode/

# OS files
.DS_Store
Thumbs.db

# CSS build (ADR-022) — dev-time only.
# node_modules is NOT committed; the BUILT stylesheet src/static/css/app.css IS,
# so running or packaging the app never needs Node.
node_modules/

# Test cache
.pytest_cache/
.coverage
htmlcov/

# Claude Code per-user settings (project-level settings.json is committed)
.claude/settings.local.json
# SQLite WAL/shared-memory sidecars (transient)
*.mfl-wal
*.mfl-shm
*.db-wal
*.db-shm
